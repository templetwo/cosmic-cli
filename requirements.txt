# Deprecated install path — prefer:
#   pip install -e ".[test]"
# Canonical deps live in pyproject.toml.
click>=8.1
python-dotenv>=1.0
xai-sdk>=1.0
rich>=13.0
pyfiglet>=1.0
textual>=0.40
requests>=2.31
