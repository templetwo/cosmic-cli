# Cosmic CLI — Audit fixes + T2Helix substrate

**Date:** 2026-07-14  
**Scope:** Security audit remediation + local memory/tasks via [t2helix](https://github.com/templetwo/t2helix)  
**Not in scope:** Direct Sovereign Stack connector (Anthony: use Helix instead)

## Goals

1. Close CRITICAL/HIGH audit items (C1–C2, H1, H3–H5, packaging).
2. Make **T2Helix** the memory + task substrate: shared SQLite chronicle with Claude seats.
3. Keep Grok path as primary executor; Helix for recall/record/goal/threads/compass.
4. **Horizon lock:** Cosmic = Temple runtime (avionics); Grok Build = cockpit. Not another TUI.
5. **PAUSE enforcement** for non-hook seats (adapter + Cosmic) — not log-only.

## Architecture

```
cosmic-cli do "…"
    │
    ├─ helix.boot(query)     → recall foundational memory + set goal
    ├─ StargazerAgent loop
    │     ├─ SHELL → shell_guard + helix.witness (compass)
    │     ├─ READ  → secrets denylist
    │     ├─ MEMORY → helix.recall
    │     └─ FINISH → helix.record mission receipt
    └─ optional --review seat
```

Helix access: Node subprocess RPC → `~/t2helix/lib/grok-adapter` + `chronicle`  
Data dir: `T2HELIX_DATA_DIR` or `~/.claude/plugins/data/t2helix-templetwo-t2helix` or `~/.t2helix-data`

## Work order

| # | Item | Audit |
|---|------|-------|
| 1 | `shell_guard` shared (case-insensitive + network denylist in safe) | H1, H2 partial |
| 2 | Secret path denylist + log redaction | H3 |
| 3 | Rewrite `ollama_agent` through tools + shell_guard | C1, C2, M8 |
| 4 | xAI `timeout=` on Client | H5 |
| 5 | Consciousness log `\n` fix | H4 |
| 6 | Packaging: drop setup.py, COSMIC_SETUP, dead requirements, demo crash | M2–M4, M11–M12 |
| 7 | Helix RPC + Python bridge | — |
| 8 | Wire boot/MEMORY/FINISH/CLI | — |
| 9 | Tests + smoke | H6 partial |

## Success criteria

- `cosmic-cli doctor` shows Helix data dir + health
- Ollama safe mode blocks `rm -rf` / `RM -RF`
- READ `.env` denied
- Mission FINISH appears in Helix chronicle via `grokRecord`
- PAUSE returns a token and blocks SHELL until `helix confirm` + retry
- Docs state PAUSE/WITNESS/OPEN honestly (no "compass protects both ✓" half-truth)
- pytest green

## Compass status (honest)

| Class | Enforced for Cosmic? | Notes |
|-------|----------------------|--------|
| WITNESS | Yes | Hard deny |
| PAUSE | Yes (as of 2026-07-14) | Token via adapter `createPendingConfirmation`; Cosmic blocks until confirm |
| OPEN | Yes | Proceed |

Previously: PAUSE was **logged only** in `grok-adapter.js` (`classification !== 'WITNESS'` → record). That is closed in the adapter + Cosmic `_run_shell`.
